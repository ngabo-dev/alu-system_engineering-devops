#!/usr/bin/env bash
# Commenting

key_name = "school"
key_bits = 4096
passphrase = "betty"

#Generate RSA key pairs
ssh-keygen -t rsa -b $key_bits -C "Generated by $USER" -f "$key_name" -N "$passphrase"

#dispalying success message
echo "RSA key pair generated successfully."
